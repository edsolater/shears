/****
 *
 * default solidjs's createContext is readonly
 * this hook is used to create a context with set() , so user can change context value
 *
 ***/
const createComponentController = (initialValue) => {
  
}
